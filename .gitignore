node_modules/
dist/
.vite/

# User-uploaded files (ID-document photos, lease documents) — never commit;
# persists across deploys since git reset --hard leaves untracked dirs alone.
uploads/
uploads-test/

# SQLite database files — never commit; exposes demo data at rest
data.db
data.db-shm
data.db-wal
data.db-journal

.env
.env.*
!.env.example

*.log
.DS_Store
.blob_cache.json
